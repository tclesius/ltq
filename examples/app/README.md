# Multi-Worker App Example

Runs email and notification workers together using `ltq.App`.

## Start the app

```bash
uv run ltq --app app.main:app
```

## Dispatch messages

```bash
uv run -m app.main
```
